/// Single ANJANAM logo raster for the whole app.
///
/// Use only this path for in-app branding. Android/iOS launcher icons are generated
/// from the same file via `flutter_launcher_icons` in [pubspec.yaml].
const String kAppBrandLogoAsset = 'assets/branding/app_icon.png';
