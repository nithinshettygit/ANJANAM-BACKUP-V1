# Copy to key.properties (same folder) and fill in. Never commit key.properties or your .jks file.
#
# Create the keystore once (example — adjust paths and validity):
#   keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
#
storePassword=Shettyn@517mannevu.store.password
keyPassword=Shettyn@517mannevu.key.password
keyAlias=upload
storeFile=upload-keystore.jks











